import React from 'react';
import ChannelList from './ChannelList';

const Chat = ({ channels}) => {

    
    

   
    return(
        <div className="">
            <h2>Here the Chat</h2>
            <ChannelList channels={channels} />
        </div>

    )
}

export default Chat;